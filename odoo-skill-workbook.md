# Odoo MCP × Claude Skill 실습 워크북

**대상** — "스킬이 뭔지" 처음 배우는 학생 / 실무자
**목표** — Odoo MCP를 프롬프트로 직접 써 보고, 그 절차를 **내 손으로 스킬 5개로 승격**한다
**소요** — 약 3시간 (시나리오당 20~40분)

---

## 이 강의에서 만드는 것

난이도를 한 칸씩 올리며 스킬 5개를 순서대로 만듭니다. 뒤로 갈수록 **앞에서 만든 스킬을 부품처럼 다시 씁니다.**

| # | 만들 스킬 | 난이도 | 새로 배우는 것 |
|:-:|---|:-:|---|
| 1 | `odoo-contact-lookup` | ★ | MCP 호출 1번을 절차로 고정하기 |
| 2 | `odoo-bulk-partner-import` | ★★ | 쓰기(write) · 중복 체크 · 부분 실패 허용 |
| 3 | `odoo-purchase-report` | ★★★ | **스킬이 다른 스킬을 부른다** (위임) |
| 4 | `odoo-procurement-dashboard` | ★★★★ | 여러 모델을 동시에 훑기 (팬아웃) |
| 5 | `odoo-reorder-request` | ★★★★★ | 스킬 재사용으로 워크플로 조립 (오케스트레이션) |

> 📁 **정답지는 [`materials/output/skills/`](materials/output/skills/)에 있습니다.**
> 먼저 직접 만들어 보고, 막히거나 다 만든 뒤에 비교해 보세요. 처음부터 열어 보면 배우는 게 없습니다.

---

## 0. 준비 — 3분

### 0-1. Odoo MCP가 붙었는지 확인

Claude Code에 그대로 쳐 보세요.

```
odoo MCP 연결됐는지 확인해줘. 등록된 모델 개수랑 res.partner 샘플 3개만 보여줘.
```

Claude가 `mcp__odoo__list_models`와 `mcp__odoo__search_records`를 호출하고, `(주)한빛전자부품` 같은 이름이 나오면 성공입니다.

**안 나오면** — `.mcp.json`의 `odoo` 서버를 신뢰할지 묻는 창을 아직 승인하지 않았거나, API 키가 안 채워진 것입니다. [README의 "빠른 시작"](README.md)을 먼저 보세요.

> ⚠️ **MCP 서버 스코프 충돌 주의**
> 이미 개인 설정(user scope)에 `odoo` 서버를 등록해 뒀다면, 이 저장소의 `.mcp.json`(프로젝트 scope)이 그것을 가려서 **둘 다 안 붙을 수 있습니다.** `claude mcp list`에 `[Conflicting scopes]` 경고가 뜨면 둘 중 하나를 지우세요.
> ```
> claude mcp remove odoo -s project     # 개인 설정을 쓰겠다
> claude mcp remove odoo -s user        # 이 저장소 설정을 쓰겠다
> ```

### 0-2. 실습 데이터가 들어 있는지 확인

```
품목이 몇 개 등록돼 있고, 발주는 몇 건인지 알려줘.
```

**품목 10개 / 발주 14건**이 나와야 정상입니다. 0건이면 시딩이 안 된 것이니 [부록 E](#부록-e--실습-데이터-시딩과-초기화)로 가세요.

> ⚠️ 이 인스턴스는 `ODOO_YOLO=true`로 **모든 모델에 읽기/쓰기가 열려 있는 실습용 샌드박스**입니다.
> 운영 Odoo에 이 워크북을 그대로 갖다 대지 마세요.

---

## 1. 개념 정리 — 프롬프트 · MCP · 스킬

세 가지를 헷갈리면 강의 내내 헤맵니다. 딱 한 번만 정리하고 갑니다.

| | 정체 | 예시 | 재사용 |
|---|---|---|:-:|
| **프롬프트** | 그때그때 치는 자연어 | "한빛전자부품 연락처 찾아줘" | ❌ 매번 다시 침 |
| **MCP** | 시스템이 제공하는 **동사(도구)** | `search_records`, `create_record` | ⭕ 단, "한 번의 동작"뿐 |
| **스킬** | 그 동사들을 엮는 **절차 + 판단 기준 + 산출물 규격** | `odoo-purchase-report` | ⭕ 이름/자연어로 불러 씀 |

> **MCP는 "무엇을 할 수 있는가", 스킬은 "우리 회사에선 그걸 이렇게 한다".**

Odoo MCP 도구를 서너 번 조합해야 끝나는 일을, 매번 도메인 필터 문법과 호출 순서를 다시 설명해 가며 프롬프트로 치는 건 낭비입니다.
**"이 순서로, 이 조건으로, 이 포맷으로"를 한 번 적어 두고 이름을 붙인 것이 스킬**이고, 그 초안을 자동으로 만들어 주는 것이 `skill-creator` 스킬입니다.

### 이 강의에서 쓸 Odoo MCP 도구 6개

| 도구 | 하는 일 | 처음 쓰는 곳 |
|---|---|:-:|
| `mcp__odoo__list_models` | 등록된 모델 목록 | 0단계 |
| `mcp__odoo__search_records` | 조건으로 레코드 조회 | 시나리오 1 |
| `mcp__odoo__create_record` | 레코드 생성 | 시나리오 2 |
| `mcp__odoo__aggregate_records` | 그룹별 집계(합계·건수) | 시나리오 3 |
| `mcp__odoo__get_record` | id로 한 건 조회 | 시나리오 5 |
| `mcp__odoo__post_message` | 레코드에 메시지(chatter) 남기기 | 시나리오 5 |

---

## 2. 실습 데이터 지도

시나리오를 풀다 막히면 여기로 돌아오세요. **시나리오의 "이렇게 나오면 성공"은 전부 이 표에서 나옵니다.**

### 거래처 — 공급사 8 / 고객 2 / 담당자 6

| 코드 | 회사 | 이메일 | 전화 | 도시 |
|---|---|---|---|---|
| SUP-01 | (주)한빛전자부품 | sales@hanbit-parts.example.com | 02-555-0101 | 성남시 |
| SUP-02 | 대한산업소재(주) | contact@daehan-materials.example.com | 041-555-0202 | 아산시 |
| SUP-03 | (주)정우정밀 | order@jw-precision.example.com | 031-555-0303 | 화성시 |
| SUP-04 | 세강케이블(주) | **없음** | 032-555-0404 | 인천시 |
| SUP-05 | (주)나노코팅테크 | sales@nanocoat.example.com | 053-555-0505 | 대구시 |
| SUP-06 | 광성베어링(주) | ks@ks-bearing.example.com | 051-555-0606 | 부산시 |
| SUP-07 | (주)에이스몰드 | **없음** | 055-555-0707 | 김해시 |
| SUP-08 | 한결포장산업 | hankyul@hankyul-pack.example.com | **없음** | 청주시 |
| CUS-01 | (주)미래모빌리티 | po@mirae-mobility.example.com | 031-555-0901 | 화성시 |
| CUS-02 | 서한전자(주) | buy@seohan-elec.example.com | 054-555-0902 | 구미시 |

담당자(회사에 소속된 개인)는 6명입니다 — 김도현(SUP-01 영업팀장), 박서연(SUP-02 구매담당), 이준호(SUP-03 생산관리, **전화 없음**), 최민아(SUP-04 영업대표, **이메일 없음**), 정태우(SUP-05 기술영업), 한지원(SUP-06 품질담당).

> 💡 **빈칸은 일부러 비워 둔 것입니다.** 실무 데이터는 항상 군데군데 비어 있고, 스킬은 그걸 "미등록"으로 처리할 줄 알아야 합니다.

### 품목 — 10개 (재주문 규칙 포함)

| 코드 | 품목 | 원가(원) | 공급사 | 리드타임 | 현재고 | 최소 | 최대 | 판정 |
|---|---|---:|:-:|---:|---:|---:|---:|:-:|
| PCB-100 | 메인 컨트롤 PCB | 85,000 | SUP-01 | 14일 | 12 | 30 | 120 | 🔴 부족 |
| SMPS-200 | SMPS 파워모듈 | 100,000 | SUP-01 | 10일 | 5 | 20 | 80 | 🔴 부족 |
| SNS-310 | 근접센서 모듈 | 42,000 | SUP-01 | 7일 | 180 | 50 | 200 | 🟢 정상 |
| ALU-400 | 알루미늄 프로파일 40×40 | 18,000 | SUP-02 | 5일 | 260 | 100 | 400 | 🟢 정상 |
| STL-410 | 스테인리스 판재 1.2T | 55,000 | SUP-02 | 9일 | 22 | 40 | 150 | 🔴 부족 |
| BRG-500 | 정밀 볼베어링 6204 | 9,500 | SUP-06 | 6일 | 200 | 200 | 600 | 🟡 **경계** |
| GER-510 | 헬리컬 기어 M2 | 76,000 | SUP-03 | 12일 | 64 | 30 | 100 | 🟢 정상 |
| CBL-600 | 하네스 케이블 2m | 12,000 | SUP-04 | 8일 | 90 | 150 | 500 | 🔴 부족 |
| CTG-700 | 세라믹 코팅제 5L | 145,000 | SUP-05 | 21일 | 31 | 10 | 40 | 🟢 정상 |
| PKG-800 | 완충 포장재 세트 | 3,200 | SUP-08 | 4일 | 240 | 300 | 1,000 | 🔴 부족 |

> 🟡 **BRG-500은 현재고 200 = 최소 200으로 딱 걸쳐 있습니다.** 부족일까요, 아닐까요?
> `<` 로 판정하면 정상, `<=` 로 판정하면 부족입니다. **사람마다 답이 달라지는 지점** — 시나리오 4에서 이걸 스킬에 못 박습니다.

### 발주 — 14건 / 총 60,500,000원

| 번호 | 거래처 | 발주일 | 품목 | 금액(원) | 상태 |
|---|---|---|---|---:|---|
| P00001 | SUP-01 | 2026-05-11 | PCB-100 ×60, SNS-310 ×100 | 9,300,000 | 확정·입고완료 |
| P00002 | SUP-02 | 2026-05-15 | ALU-400 ×300 | 5,400,000 | 확정·입고완료 |
| P00003 | SUP-04 | 2026-05-22 | CBL-600 ×400 | 4,800,000 | 확정·입고완료 |
| P00004 | SUP-01 | 2026-06-02 | SMPS-200 ×50 | 5,000,000 | 확정·입고완료 |
| P00005 | SUP-06 | 2026-06-05 | BRG-500 ×500 | 4,750,000 | 확정·입고완료 |
| P00006 | SUP-03 | 2026-06-09 | GER-510 ×40 | 3,040,000 | 확정·입고완료 |
| P00007 | SUP-02 | 2026-06-16 | STL-410 ×80 | 4,400,000 | 확정·입고완료 |
| P00008 | SUP-05 | 2026-06-23 | CTG-700 ×20 | 2,900,000 | 확정·입고완료 |
| P00009 | SUP-01 | 2026-07-01 | SNS-310 ×150 | 6,300,000 | 확정·입고완료 |
| P00010 | SUP-08 | 2026-07-06 | PKG-800 ×800 | 2,560,000 | 확정·입고완료 |
| P00011 | SUP-03 | 2026-07-13 | GER-510 ×25 | 1,900,000 | 확정·**미착** |
| P00012 | SUP-02 | 2026-07-20 | ALU-400 ×150, STL-410 ×30 | 4,350,000 | 확정·**미착** |
| P00013 | SUP-01 | 2026-07-24 | PCB-100 ×40 | 3,400,000 | **RFQ(draft)** |
| P00014 | SUP-04 | 2026-07-28 | CBL-600 ×200 | 2,400,000 | **RFQ(draft)** |

**거래처별 합계** (시나리오 3의 정답):

| 거래처 | 건수 | 합계(원) |
|---|---:|---:|
| (주)한빛전자부품 | 4 | 24,000,000 |
| 대한산업소재(주) | 3 | 14,150,000 |
| 세강케이블(주) | 2 | 7,200,000 |
| (주)정우정밀 | 2 | 4,940,000 |
| 광성베어링(주) | 1 | 4,750,000 |
| (주)나노코팅테크 | 1 | 2,900,000 |
| 한결포장산업 | 1 | 2,560,000 |
| **합계** | **14** | **60,500,000** |

> 세금은 일부러 0으로 비워 뒀습니다 — 실습 중 숫자가 딱 떨어져야 "맞게 나왔나"를 바로 알 수 있기 때문입니다.

---

# 시나리오 1 — 거래처 연락처 조회

### 난이도 ★ · 만들 스킬 `odoo-contact-lookup` · 약 20분

| | |
|---|---|
| **새로 배우는 것** | 필드·표 형식을 스킬에 고정하기 (그리고 형식이 절차를 바꾸는 것 보기) |
| **쓰는 MCP 도구** | `search_records` ×2 (회사 → 소속 담당자) |
| **부르는 다른 스킬** | 없음 |
| **성격** | 읽기 전용 |

### ① 상황

구매팀 세 사람이 같은 질문을 합니다 — "정우정밀 연락처 좀".
A는 이름과 전화만 뽑고, B는 이메일까지 뽑고, C는 담당자 이름을 빼먹습니다. **같은 질문인데 결과가 매번 다릅니다.**

### ② 이렇게 쳐 보세요

```
정밀 관련 공급업체 연락처 찾아줘
```

이어서 조건을 바꿔 한 번 더:

```
이번엔 이메일이 등록 안 된 거래처만 뽑아줘
```

한 번 더:

```
공급사만, 회사 단위로 전부 보여줘
```

### ③ Claude가 하는 일

```python
# 첫 번째
mcp__odoo__search_records(
  model="res.partner",
  domain=[["name", "ilike", "정밀"]],
  fields=["name", "email", "phone", "is_company"])

# 두 번째 — 같은 도구, domain만 다름
mcp__odoo__search_records(
  model="res.partner",
  domain=[["email", "=", False]], ...)

# 세 번째
mcp__odoo__search_records(
  model="res.partner",
  domain=[["supplier_rank", ">", 0], ["is_company", "=", True]], ...)
```

### ④ 이렇게 나오면 성공

- 첫 번째 → **(주)정우정밀** (+ 담당자 이준호)
- 두 번째 → **세강케이블(주) / (주)에이스몰드 / 최민아** 3건
- 세 번째 → **공급사 8개** (SUP-01 ~ SUP-08)

### ⑤ 스킬로 만들기

이제 세 번의 프롬프트에서 **매번 손으로 다시 조립한 `fields` 목록**을 고정할 차례입니다. 아래 한 문장이면 됩니다.

```
지금까지 한 것처럼 "거래처 연락처 조회", "공급업체 조회" 등이 사용되면
매번 같은 필드(이름/회사여부/이메일/전화)를 가지는 테이블 하나로 보여주는
스킬을 등록해 줘. 담당자는 별도 행이 아니라 컬럼(담당자/담당자 이메일/담당자 전화)으로
회사 행에 붙여줘. 이메일/전화가 비어 있으면 "미등록"으로 표시하도록 명시해줘.
```

> 💡 **"담당자는 컬럼으로"라는 한 줄이 조회 횟수를 바꿉니다.** 회사와 담당자는 `res.partner`의
> 별개 레코드라, 담당자를 컬럼에 채우려면 회사를 찾은 뒤 `parent_id`로 한 번 더 조회해야 합니다.
> 출력 형식을 한 줄 지정했을 뿐인데 절차가 따라 바뀌는 것 — 스킬이 "포맷"이 아니라
> **"절차 + 포맷"**인 이유입니다.

### 만들어진 스킬 테스트

**만들었으면 반드시 써 보세요.** 두 가지 방식으로 확인합니다.

**1) 자연어로 부르기** — 스킬 이름을 대지 않습니다. 이게 스킬의 본래 사용법입니다.

```
광성베어링 연락처 알려줘
```

기대 결과 — 표 하나에, 담당자 한지원까지 **같은 행에** 붙어서 나와야 합니다.

| 이름 | 회사여부 | 이메일 | 전화 | 담당자 | 담당자 이메일 | 담당자 전화 |
|---|---|---|---|---|---|---|
| 광성베어링(주) | 회사 | ks@ks-bearing.example.com | 051-555-0606 | 한지원 | jw.han@ks-bearing.example.com | 010-2222-0606 |

한지원이 따로 아래에 떨어져 나오거나 아예 빠졌다면, 스킬이 `parent_id` 2차 조회를 건너뛴 것입니다.

**2) 슬래시로 강제 호출** — 자연어로 안 걸릴 때 확인용입니다.

```
/<만들어진-스킬-이름> 이메일 없는 거래처
```

기대 결과 — **2행**이 나오고, 빈 이메일 자리 **3곳**에 전부 "미등록"이 찍혀야 합니다.

| 이름 | 회사여부 | 이메일 | 전화 | 담당자 | 담당자 이메일 | 담당자 전화 |
|---|---|---|---|---|---|---|
| 세강케이블(주) | 회사 | **미등록** | 032-555-0404 | 최민아 | **미등록** | 010-2222-0404 |
| (주)에이스몰드 | 회사 | **미등록** | 055-555-0707 | 없음 | — | — |

빈칸이나 `false`로 나오면 스킬이 아직 그 지시를 안 따르는 것이니 SKILL.md를 고치세요.

> 💡 **"이메일 없는 것 3건"이 2행에 담긴 것을 보세요.** 최민아는 세강케이블(주)의 담당자 컬럼
> 안에 들어가 있습니다. 담당자를 컬럼으로 올린 대가로, **"몇 건"이 곧 "몇 행"이 아니게** 됩니다.
> 건수를 세어야 하는 요청이라면 스킬에 "이때는 담당자를 행으로 놓는다"는 예외를 적어야 합니다.

> 💡 **트리거 확인이 이 테스트의 절반입니다.** 1)에서 스킬이 안 걸리고 Claude가 그냥 알아서 답했다면, 스킬은 있으나 마나입니다. `description`에 사용자가 실제로 쓸 표현이 부족한 것이니 거기에 표현을 더 넣으세요.

> ⚠️ **스킬 이름은 다르게 나올 수 있습니다.** 위 프롬프트에는 이름을 지정하지 않았으니 Claude가 알아서 짓습니다.
> 이 워크북은 `odoo-contact-lookup` 기준으로 쓰여 있으니, 다른 이름이 나왔다면 그 이름으로 바꿔 달라고 하거나, 이후 시나리오에서 `odoo-contact-lookup`을 실제 이름으로 바꿔 읽으세요.

### ⑥ 여기서 배운 것

- 스킬의 최소 형태는 **"MCP 호출 + 필드/포맷 고정"**이다
- 스킬의 값어치는 새 기능이 아니라 **결과의 일관성**에 있다
- **출력 형식을 바꾸면 절차도 따라 바뀐다** — "담당자를 컬럼으로" 한 줄이 조회를 2번으로 만들었다
- SKILL.md에는 "무엇을 하는지"뿐 아니라 **"무엇을 하지 말아야 하는지"**도 적는다

---

# 시나리오 2 — 신규 협력사 대량 등록

### 난이도 ★★ · 만들 스킬 `odoo-bulk-partner-import` · 약 30분

| | |
|---|---|
| **새로 배우는 것** | 쓰기(write) 스킬 · 중복 체크 · 부분 실패 허용 |
| **쓰는 MCP 도구** | `search_records` + `create_record` |
| **부르는 다른 스킬** | (엑셀 읽기) |
| **성격** | **쓰기 — 실제 데이터가 생깁니다** |

### ① 상황

전시회에서 받아 온 신규 협력사 6곳을 Odoo에 넣어야 합니다. 담당자가 화면을 띄워 놓고 한 줄씩 손으로 칩니다. 30분. 게다가 그중 하나는 **이미 등록된 회사**라 중복이 생깁니다.

### ② 이렇게 쳐 보세요

실습 파일: [`materials/신규협력사.xlsx`](materials/신규협력사.xlsx) — 컬럼은 `회사명 | 이메일 | 전화 | 담당자`, 6행입니다.

```
materials/신규협력사.xlsx 에 있는 신규 협력사를 Odoo 거래처로 등록해줘.
이미 등록된 이름이면 건너뛰고, 등록 결과를 요약해줘.
```

### ③ Claude가 하는 일 (행마다 반복)

```python
# 1) 중복 확인 — 이걸 빼먹으면 같은 회사가 두 번 생깁니다
mcp__odoo__search_records(model="res.partner",
  domain=[["name", "=", 행.회사명]])
  → 있으면 스킵

# 2) 없으면 생성
mcp__odoo__create_record(model="res.partner",
  values={"name": 행.회사명, "email": 행.이메일,
          "phone": 행.전화, "supplier_rank": 1})

# 3) 한 행이 실패해도 멈추지 않고 다음 행으로

# 4) 마지막에 "등록 N건 / 스킵 M건 / 실패 K건" 요약
```

### ④ 이렇게 나오면 성공

엑셀 6행은 일부러 이렇게 구성해 뒀습니다.

| # | 회사명 | 기대 결과 | 왜 |
|:-:|---|---|---|
| 1 | (주)태성정공 | ✅ 등록 | 신규 |
| 2 | 유진하이테크(주) | ✅ 등록 | 신규 |
| 3 | (주)정우정밀 | ⏭️ **스킵** | SUP-03으로 이미 있음 |
| 4 | 남광열처리 | ✅ 등록 | 이메일 빈칸이지만 회사명이 있으니 등록 가능 |
| 5 | *(빈칸)* | ❌ **실패** | 필수값(회사명) 없음 |
| 6 | 대성EMC(주) | ✅ 등록 | 신규 |

**최종 요약: 등록 4건 / 스킵 1건 / 실패 1건**

> 💡 **5행에서 멈추지 않고 6행까지 갔는지 꼭 확인하세요.** 이게 "부분 실패 허용"입니다.
> 중간에 멈추는 자동화는 손으로 하는 것보다 나쁩니다 — 어디까지 됐는지 아무도 모르니까요.

### ⑤ 스킬로 만들기

```
방금 한 "엑셀 → Odoo 거래처 대량 등록 + 중복 체크 + 결과 요약" 과정을
skill-creator로 odoo-bulk-partner-import 스킬로 만들어줘.

- 컬럼명이 엑셀마다 다를 수 있으니 컬럼 매핑을 유연하게 처리해줘
  (회사명/거래처명/업체명 → name 처럼).
- 생성 전에 반드시 search_records로 중복 확인하는 단계를 절차에 못 박아줘.
- 실패한 행이 있어도 나머지는 계속 진행하고, 마지막에 등록/스킵/실패를
  표로 요약하게 해줘.
- 실습용으로 만든 레코드를 되돌리는 방법도 스킬에 적어줘.
```

### ⑥ 여기서 배운 것

- 사람이 엑셀 보며 한 줄씩 치던 일을 그대로 옮긴 것이 이 스킬의 원형
- **중복 확인 → 생성 → 실패해도 계속 → 결과 요약**, 이 4단계 절차가 스킬의 값어치
- 쓰기 스킬에는 반드시 **되돌리는 방법**이 함께 적혀 있어야 한다

---

# 시나리오 3 — 발주 현황 집계 리포트

### 난이도 ★★★ · 만들 스킬 `odoo-purchase-report` · 약 30분

| | |
|---|---|
| **새로 배우는 것** | **스킬이 다른 스킬을 부른다** (위임, delegate) |
| **쓰는 MCP 도구** | `aggregate_records` (+ `search_records`) |
| **부르는 다른 스킬** | `docx` |
| **성격** | 읽기 전용 + 문서 생성 |

### ① 상황

월말마다 "거래처별 발주 실적" 보고서를 워드로 만듭니다. 숫자는 Odoo에 다 있는데, 매번 사람이 옮겨 적고 표 서식을 다시 맞춥니다.

### ② 이렇게 쳐 보세요

```
지금까지 발주 현황을 거래처별로 집계해서 워드 문서 보고서로 만들어줘
```

### ③ Claude가 하는 일

```python
# 1) 숫자는 Odoo가 계산 — 레코드를 다 끌어와 직접 더하지 않습니다
mcp__odoo__aggregate_records(
  model="purchase.order",
  groupby=["partner_id"],
  aggregates=["amount_total:sum", "__count"])

# 2) (선택) 품목 단위 상세
mcp__odoo__search_records(model="purchase.order.line",
  fields=["order_id", "product_id", "product_qty", "price_subtotal"])

# 3) 문서 서식은 docx 스킬에 위임 ← 여기가 핵심
Skill(docx)
```

### ④ 이렇게 나오면 성공

집계 결과가 [실습 데이터 지도의 거래처별 합계](#발주--14건--총-60500000원)와 일치해야 합니다.

```
(주)한빛전자부품   4건   24,000,000원   ← 1위
대한산업소재(주)   3건   14,150,000원
세강케이블(주)     2건    7,200,000원
(주)정우정밀       2건    4,940,000원
광성베어링(주)     1건    4,750,000원
(주)나노코팅테크   1건    2,900,000원
한결포장산업       1건    2,560,000원
────────────────────────────────
합계              14건   60,500,000원
```

**그리고 도구 호출 로그를 꼭 보세요.** `mcp__odoo__aggregate_records` 다음 줄에 **`Skill(docx)` 호출이 따로 찍혀 있어야 합니다.** 이게 이 시나리오의 전부입니다.

> 참고 산출물: [`materials/output/purchase-report.docx`](materials/output/purchase-report.docx)

### ⑤ 스킬로 만들기

```
방금 한 "purchase.order를 거래처별로 집계 → docx로 보고서 생성" 과정을
skill-creator로 odoo-purchase-report 스킬로 만들어줘.

스킬 본문에 이 두 가지를 반드시 명시해줘:
1. 데이터는 aggregate_records로 직접 뽑는다 (레코드를 전부 조회해 직접 합산하지 않는다)
2. 문서 생성은 반드시 docx 스킬에 위임한다 — 이 스킬 안에 워드 서식 코드를 넣지 않는다
```

### ⑥ 여기서 배운 것 — 스킬 조합의 기본형: **위임(delegate)**

> **도메인 스킬(Odoo를 아는 것)과 표현 스킬(문서를 아는 것)을 한 덩어리로 만들지 마라.**
>
> - `docx` 스킬은 **Odoo를 몰라야** 다른 프로젝트에서도 재사용된다
> - `odoo-purchase-report`는 **워드 문법을 몰라야** "이번엔 PPT로" 요청에 pptx 스킬로 갈아탈 수 있다

한 덩어리로 만들면, PPT 요청 한 번에 스킬을 통째로 다시 써야 합니다.

---

# 시나리오 4 — 조달 현황 대시보드

### 난이도 ★★★★ · 만들 스킬 `odoo-procurement-dashboard` · 약 40분

| | |
|---|---|
| **새로 배우는 것** | 여러 모델을 동시에 훑기(팬아웃) · 판단 기준을 스킬에 못 박기 |
| **쓰는 MCP 도구** | `search_records` ×3 + `aggregate_records` |
| **부르는 다른 스킬** | `pptx` (차트·슬라이드 모두) |
| **성격** | 읽기 전용 + 시각화 |

### ① 상황

경영진 보고 자료를 만들어야 합니다. 필요한 숫자가 **서로 다른 4개 모델**에 흩어져 있습니다 — 재주문 규칙, 품목 재고, 발주, 거래처.

### ② 이렇게 쳐 보세요

```
재고 부족 품목, 거래처별 발주 현황, 협력사 현황을 모아서
조달 현황 보고 대시보드 만들어줘
```

### ③ Claude가 하는 일

```python
# 1) 재고 부족 — 모델 2개를 조인해야 나옵니다
mcp__odoo__search_records(model="stock.warehouse.orderpoint",
  fields=["product_id", "product_min_qty", "product_max_qty"])
mcp__odoo__search_records(model="product.product",
  fields=["name", "default_code", "qty_available"])
  → product_id로 조인 → qty_available < product_min_qty 인 것이 "부족"
  → 보충 필요량 = product_max_qty - qty_available

# 2) 발주 현황
mcp__odoo__aggregate_records(model="purchase.order",
  groupby=["partner_id"], aggregates=["amount_total:sum", "__count"])

# 3) 협력사 현황
mcp__odoo__search_records(model="res.partner",
  domain=[["supplier_rank", ">", 0]], fields=["name", "email", "phone"])

# 4) 차트와 슬라이드는 모두 pptx 스킬에 위임
```

### ④ 이렇게 나오면 성공

**재고 부족 5건**이 잡혀야 합니다.

| 코드 | 품목 | 현재고 | 최소 | 보충 필요 |
|---|---|---:|---:|---:|
| SMPS-200 | SMPS 파워모듈 | 5 | 20 | **75** |
| PCB-100 | 메인 컨트롤 PCB | 12 | 30 | **108** |
| STL-410 | 스테인리스 판재 1.2T | 22 | 40 | **128** |
| CBL-600 | 하네스 케이블 2m | 90 | 150 | **410** |
| PKG-800 | 완충 포장재 세트 | 240 | 300 | **760** |

### 🟡 여기서 반드시 나오는 질문 — BRG-500은?

**정밀 볼베어링 6204는 현재고 200, 최소 200으로 정확히 같습니다.**

- `qty_available < product_min_qty` → **정상** (부족 5건)
- `qty_available <= product_min_qty` → **부족** (부족 6건)

둘 다 말이 됩니다. 문제는 **사람마다 다르게 판단한다는 것**입니다. 오늘은 5건, 내일은 6건이 나오면 아무도 그 보고서를 믿지 않습니다.

> 🎯 **그래서 판단 기준을 SKILL.md에 못 박습니다.**
> 어느 쪽을 고르든 상관없습니다. 중요한 건 **한 곳에 적혀 있고, 모두가 같은 답을 얻는다**는 것입니다.
> 기준이 바뀌면 사람을 교육하는 게 아니라 **SKILL.md를 고칩니다.** 이게 스킬이 문서와 다른 점입니다.

### 💡 만약 pptx 스킬이 없다면

이 환경에 `pptx` 스킬이 설치돼 있지 않을 수 있습니다. 그때 스킬이 **즉석에서 새로 판단하게 두지 마세요.**

SKILL.md에 미리 이렇게 적어 둡니다.

```
5. 최종 산출물 조립
   - pptx 스킬이 있으면 → 차트까지 포함한 슬라이드로
   - 없으면 → 차트 없이 표 중심 문서(docx)나 대화 내 표로 대체하고,
     "pptx가 없어 차트를 뺐다"고 사용자에게 먼저 알린다
     (1~3에서 모은 데이터를 그대로 재사용한다)
```

> **차트 경로를 pptx 하나로 몰아둔 이유** — 급할 때마다 다른 도구로 차트를 그리면
> 같은 지표가 자료마다 다른 모양으로 나옵니다. 표현을 한 곳에 위임한 의미가 사라지죠.
> 도구가 없으면 **차트를 빼지, 다른 방식으로 급조하지 않습니다.**

> **예외 상황까지 미리 적어 두는 것이 스킬 본문의 핵심 역할입니다.** 그래야 없는 도구를 만났을 때도 결과가 흔들리지 않습니다.

### ⑤ 스킬로 만들기

```
방금 한 "재고부족 + 거래처별 발주현황 + 협력사현황을 모아 대시보드로" 과정을
skill-creator로 odoo-procurement-dashboard 스킬로 만들어줘.

- 데이터 수집(Odoo MCP 3~4회)과 표현(pptx 위임)을 단계별로 분리해서 적어줘.
  차트도 pptx가 그리게 하고, 이 스킬은 차트를 직접 그리지 않게 해줘.
- 재고 부족 판정 기준을 "qty_available < product_min_qty"로 명시하고,
  현재고 == 최소재고인 경계 케이스는 정상으로 본다고 못 박아줘.
- 보충 필요량은 product_max_qty - qty_available 로 계산한다고 적어줘.
- pptx 스킬이 없을 때의 대안 경로도 5단계에 미리 적어줘.
```

### ⑥ 여기서 배운 것

- 스킬은 MCP 호출 하나가 아니라 **여러 데이터 소스 + 여러 스킬을 지휘하는 파이프라인**이 될 수 있다
- **반복되는 계산 기준(부족 판정, 보충량)은 스킬이 고정해야** 사람마다 결과가 달라지지 않는다
- **예외 상황(도구 없음)을 미리 적어 두면** 스킬이 즉흥적으로 판단하지 않는다

---

# 시나리오 5 — 재고 부족 감지 → 발주요청서 → 승인 → 실발주

### 난이도 ★★★★★ · 만들 스킬 `odoo-reorder-request` · 약 40분

| | |
|---|---|
| **새로 배우는 것** | **이미 만든 스킬을 부품처럼 재사용해 워크플로 조립** |
| **쓰는 MCP 도구** | 위 전부 + `create_record` + `post_message` |
| **부르는 다른 스킬** | `odoo-procurement-dashboard`(판정), `odoo-purchase-report`(문서) |
| **성격** | **쓰기 — 실제 발주가 생깁니다. 사람 승인 필수** |

### ① 상황

재고가 떨어졌습니다. 담당자가 (1) 부족 품목을 확인하고 (2) 발주요청서를 쓰고 (3) 팀장 결재를 받고 (4) Odoo에 발주를 등록하고 (5) 관련자에게 알립니다. **5단계 전부가 이미 시나리오 1~4에서 조각조각 나왔습니다.**

### ② 5단계를 순서대로

#### Step 1 — 진단

```
재고 부족 품목 체크해줘
```

시나리오 4에서 **못 박아 둔 판정 기준을 그대로 재사용**합니다. 새 기준을 만들지 않습니다.
→ 부족 5건, 가장 시급한 것은 **SMPS-200 (현재고 5 / 최소 20)**

#### Step 2 — 발주요청서 문서 생성

```
부족한 품목으로 발주요청서 문서 만들어줘
```

여기서 Claude는 **새로 문서 서식을 짜지 않습니다.** 시나리오 3에서 만든 `odoo-purchase-report`(또는 그것이 쓰는 docx 위임 절차)를 그대로 다시 부릅니다.
→ 참고 산출물: [`materials/output/reorder-request.docx`](materials/output/reorder-request.docx)

#### Step 3 — 사람 승인 ⛔

```
SMPS-200 75개를 (주)한빛전자부품에 발주해줘. 나머지는 보류.
```

> ⛔ **스킬이 여기서 반드시 멈춰야 합니다.**
> 재고 부족을 감지했다고 자동으로 발주가 나가면, 잘못된 판정 한 번이 곧바로 돈이 나가는 사고가 됩니다.
> Claude가 승인 없이 Step 4로 넘어갔다면 **그건 스킬 버그입니다.** SKILL.md를 고치세요.

수량 75 = 최대재고 80 − 현재고 5 (시나리오 4에서 정한 보충량 공식 그대로)
공급사 = SUP-01 (주)한빛전자부품 (품목에 등록된 기본 공급사, 단가 100,000원)

#### Step 4 — 실제 발주 등록

시나리오 2에서 익힌 **"생성 전 확인 → 생성 → 결과 확인"** 패턴을 그대로 씁니다.

```python
mcp__odoo__create_record(model="purchase.order",
  values={"partner_id": <SUP-01의 id>, "date_order": "<오늘>"})

mcp__odoo__create_record(model="purchase.order.line",
  values={"order_id": <위에서 만든 id>,
          "product_id": <SMPS-200의 id>,
          "product_qty": 75,
          "price_unit": 100000})

mcp__odoo__get_record(model="purchase.order", record_id=<id>)
  → amount_total 7,500,000원 / state "draft" 확인
```

#### Step 5 — 알림

```python
mcp__odoo__post_message(model="purchase.order", res_id=<새 발주 id>,
  body="SMPS-200 75개 발주 등록 완료(7,500,000원). 담당자 확인 요청")
```

### ③ 이렇게 나오면 성공

- ✅ Step 2에서 **새 문서 서식을 짜지 않고** 기존 스킬을 재호출했다
- ✅ Step 3에서 **실제로 멈춰서 사람에게 물었다** (자동으로 넘어가지 않았다)
- ✅ Step 4의 절차가 시나리오 2의 생성 패턴과 **동일하다**
- ✅ 새 발주가 `state="draft"`, `amount_total=7,500,000`으로 생성됐다

### 🧹 실습 후 정리

```
방금 만든 발주(SMPS-200 75개)를 삭제해서 실습 전 상태로 돌려줘.
```

> draft 상태라 회계·재고에 실제 영향은 없습니다. 다음 기수를 위해 재고 부족 상태(SMPS-200 = 5)는 **그대로 두세요.**

### ④ 스킬로 만들기

```
방금 한 전체 과정 — 재고 부족 감지 → (odoo-purchase-report 스킬로) 발주요청서 생성 →
사람 승인 → (odoo-bulk-partner-import에서 쓴 생성 패턴으로) 실제 발주 등록 →
담당자 알림 — 을 skill-creator로 odoo-reorder-request 스킬로 만들어줘.

스킬 본문에 이 세 가지를 반드시 명시해줘:
1. 부족 판정 기준은 odoo-procurement-dashboard와 동일하게 유지한다
2. 문서 생성이 필요하면 odoo-purchase-report 스킬을 호출한다 (서식을 새로 짜지 않는다)
3. 사람 승인 없이는 절대 4단계(실발주 등록)로 넘어가지 않는다
```

### ⑤ 여기서 배운 것 — 오케스트레이터 스킬

- **오케스트레이터 스킬은 자기가 일을 다 하지 않는다.** 이미 만든 스킬을 부품처럼 다시 부른다
- 외부 워크플로 엔진 없이도, **스킬 재사용만으로 새 업무 흐름이 조립된다**
- **승인 지점을 스킬이 미리 설계해 둔다** — 자동화의 품질은 "어디서 멈추는가"로 결정된다

> 📌 이 절차가 조직의 정식 업무가 되어 담당자·기한·결재선이 필요해지면, 그때는 BPMN 프로세스로 승격시키는 다음 단계가 있습니다. 이 워크북은 **스킬-스킬 재사용까지**만 다룹니다.

---

# 마무리

## 난이도 사다리 한눈에

| # | 스킬 | 난이도 | 새로 배운 것 | 주 MCP 도구 | 부르는 스킬 |
|:-:|---|:-:|---|---|---|
| 1 | `odoo-contact-lookup` | ★ | 필드/포맷 고정 | `search_records` ×2 | — |
| 2 | `odoo-bulk-partner-import` | ★★ | 중복체크 · 부분실패 허용 | `search_records` + `create_record` | (엑셀) |
| 3 | `odoo-purchase-report` | ★★★ | **위임** | `aggregate_records` | `docx` |
| 4 | `odoo-procurement-dashboard` | ★★★★ | **팬아웃 · 판정 기준 고정** | `search_records`×3 + `aggregate_records` | `pptx` |
| 5 | `odoo-reorder-request` | ★★★★★ | **오케스트레이션 · 승인 지점** | 위 전부 + `create_record` + `post_message` | 3번, 4번 스킬 |

## 부록 A — 스킬로 만들 값어치가 있는지 판별하는 3문항

1. **두 번 이상 같은 순서로 했는가?** → 아니면 그냥 프롬프트로 충분합니다
2. **사람마다 결과가 달라지면 곤란한가?** → 그렇다면 판단 기준을 스킬에 못 박으세요 (예: 재고 부족 판정)
3. **다른 사람에게 설명해야 하는가?** → 그 설명문이 곧 SKILL.md 본문입니다

## 부록 B — skill-creator에게 잘 시키는 법

- "방금 한 대화를 스킬로 만들어줘"만 던지지 마세요. 이 세 가지를 프롬프트에서 **직접 지정**하면 초안 품질이 확 달라집니다.
  1. **트리거 표현** — description에 들어갈 자연어 5개 이상
  2. **위임할 다른 스킬 이름** — "문서는 docx 스킬에"
  3. **하지 말아야 할 것** — "승인 없이 넘어가지 않는다", "서식 코드를 직접 넣지 않는다"
- 위 5개 시나리오의 "스킬로 만들기" 프롬프트가 그 예시입니다.
- `skill-creator`가 없다면 먼저 설치하거나, 같은 내용을 직접 SKILL.md로 적어도 결과는 같습니다 — 구조는 `frontmatter(name/description)` + `절차 본문`이 전부입니다.

## 부록 C — 흔한 안티패턴

| 안티패턴 | 왜 나쁜가 | 대신 |
|---|---|---|
| MCP 도구 하나를 그대로 감싼 스킬 | 부가가치 0 | 최소 2~3 호출을 엮고 **판단 기준**을 넣어라 |
| 도메인 로직 + 문서 서식을 한 스킬에 | PPT 요청 오면 통째로 재작성 | 도메인/표현 분리 (시나리오 3) |
| 승인 없이 대량 쓰기 | 잘못된 데이터가 그대로 들어감 | 중복/유효성 확인 → 사람 확인 → 실행 (시나리오 2, 5) |
| description이 한 줄 | 자연어로 트리거가 안 됨 | 사용자가 실제로 쓸 표현 5개 이상 |
| 예외 상황을 안 적음 | 도구가 없으면 즉흥 판단 | "없으면 이렇게 한다"를 본문에 (시나리오 4) |
| 되돌리는 방법이 없음 | 실습/사고 후 복구 불가 | 쓰기 스킬엔 정리 절차를 함께 (시나리오 2, 5) |

## 부록 D — Odoo 19에서 실제로 걸린 함정들

이 워크북의 데이터를 심으면서 **실제로 부딪힌** 것들입니다. 스킬을 만들 때 미리 알고 있으면 시간을 아낍니다.

| 증상 | 원인 | 해결 |
|---|---|---|
| `Invalid field 'mobile'` | Odoo 19에서 `res.partner.mobile` 삭제됨 (`phone`으로 통합) | `fields`에서 제거. 필드 존재 여부를 먼저 확인 |
| 회사로 만들었는데 개인으로 뜸 | `is_company`가 **생성 시엔 무시되고 수정 시에만 반영**됨 | 생성 후 `update_record`로 한 번 더 지정 |
| 재고를 넣었는데 `qty_available`이 0 | 품목이 `type="consu"` + `is_storable=True`여야 재고 추적됨 | 두 필드를 함께 지정 |
| `read_group` 메서드 없음 | Odoo 19에서 제거됨 | MCP의 `aggregate_records`를 쓰거나 `formatted_read_group` |
| 실습 중에 발주가 저절로 생김 | 재주문 규칙의 `trigger="auto"`를 Odoo 스케줄러가 실행 | 실습 데이터는 `trigger="manual"`로 |
| 발주 금액이 딱 안 떨어짐 | 세금이 자동으로 붙음 | 실습 데이터는 세금을 비워 둠 (`tax_ids` 비움) |

## 부록 E — 실습 데이터 시딩과 초기화

### 처음부터 다시 심기

[`materials/seed/seed_workshop_data.py`](materials/seed/seed_workshop_data.py)를 실행하면 이 워크북이 기대하는 상태가 그대로 만들어집니다. **여러 번 돌려도 안전합니다** (이름/코드로 먼저 찾고 없을 때만 생성).

```powershell
# PowerShell
$env:ODOO_URL="https://<instance>.odoo.com"
$env:ODOO_DB="<db>"
$env:ODOO_USER="<login-email>"
$env:ODOO_API_KEY="<api-key>"
python materials/seed/seed_workshop_data.py
```

```bash
# bash
ODOO_URL=... ODOO_DB=... ODOO_USER=... ODOO_API_KEY=... \
  python materials/seed/seed_workshop_data.py
```

### 기수 사이에 되돌려야 할 것

실습으로 **새로 생기는** 데이터는 두 가지뿐입니다.

| 시나리오 | 생기는 것 | 정리 |
|:-:|---|---|
| 2 | 거래처 4건 (태성정공 / 유진하이테크 / 남광열처리 / 대성EMC) | 삭제 |
| 5 | 발주 1건 (SMPS-200 75개, draft) | 삭제 |

품목 재고와 재주문 규칙은 건드리지 마세요 — **재고 부족 상태 자체가 시나리오 4, 5의 실습 재료**입니다.

## 부록 F — 강사용 진행 노트

| 구간 | 시간 | 놓치면 안 되는 것 |
|---|---:|---|
| 0~1장 (준비 + 개념) | 15분 | "MCP는 동사, 스킬은 절차" 한 문장 |
| 시나리오 1 | 20분 | **스킬 이름을 대지 않고 자연어로 불러 보게 할 것.** 안 걸리면 description 문제라는 걸 그 자리에서 짚어줄 것 |
| 시나리오 2 | 30분 | 5행에서 안 멈추고 6행까지 가는지 확인시킬 것 |
| 시나리오 3 | 30분 | **도구 호출 로그에서 `Skill(docx)`가 별도로 찍히는 것을 눈으로 보게 할 것** |
| 시나리오 4 | 40분 | BRG-500 경계 케이스로 토론 유도 → 기준을 못 박는 이유 |
| 시나리오 5 | 40분 | Step 3에서 진짜로 멈추는지 확인. 안 멈추면 그건 버그라고 짚어줄 것 |
| 마무리 | 15분 | 부록 A 3문항으로 "내 업무 중 뭘 스킬로 만들까" 각자 하나씩 |

**미리 준비할 것**

- [ ] 시딩 완료 (부록 E) — 품목 10 / 발주 14 확인
- [ ] `.mcp.json` 스코프 충돌 확인 (`claude mcp list`)
- [ ] `docx`, `pptx` 스킬 설치 여부 확인 — 없으면 시나리오 3, 4의 대안 경로 안내
- [ ] 이전 기수의 잔여 데이터 정리
